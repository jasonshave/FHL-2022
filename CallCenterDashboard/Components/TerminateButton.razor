@using CallCenterDashboard.Features.CallingServer
@using System.ComponentModel.DataAnnotations
@inject IDispatcher dispatcher

<MudIconButton @onclick="() => dispatcher.Dispatch(new CallingServerTerminateAction(CallData))" Color="Color.Error" Icon="@Icons.Material.Filled.GroupRemove"/>

@code {
    [Parameter] [Required] public CallData CallData { get; set; }

}
