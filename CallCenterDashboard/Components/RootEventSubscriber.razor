@using CallCenterDashboard.Features.CallingServer
@inject IDispatcher dispatcher
@inject IState<CallingServerState> callingServerState

@code {
    protected override void OnInitialized()
    {
        if(!callingServerState.Value.Initialized)
            dispatcher.Dispatch(new CallingServerInitializeAction());

        base.OnInitialized();
    }

}
