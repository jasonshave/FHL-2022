@using CallCenterDashboard.Features.CallingServer
@using System.ComponentModel.DataAnnotations
@inject IDispatcher dispatcher

<MudIconButton @onclick="() => dispatcher.Dispatch(new CallingServerTerminateAction(CallData))" Icon="@Icons.Material.Filled.PhoneDisabled"/>

@code {
    [Parameter] [Required] public CallData CallData { get; set; }

}
